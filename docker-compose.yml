version: '3.8'

services:
  # ==========================================
  # Backend API Service (Private - Port 5000 exposed internally only)
  # ==========================================
  backend:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: expensetracker_backend
    restart: always
    expose:
      - "5000"
    env_file:
      - ./server/.env
    environment:
      - PORT=5000
      - NODE_ENV=production
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:5000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - expensetracker_net

  # ==========================================
  # Frontend Service (Public - Nginx on Port 80, proxying /api to backend:5000)
  # ==========================================
  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
      args:
        VITE_API_URL: /api
    container_name: expensetracker_frontend
    restart: always
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - expensetracker_net

networks:
  expensetracker_net:
    driver: bridge
